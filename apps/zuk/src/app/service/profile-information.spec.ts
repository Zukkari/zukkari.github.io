import { ProfileInformation } from './profile-information';

describe('ProfileInformation', () => {
  it('should create an instance', () => {
    expect(new ProfileInformation()).toBeTruthy();
  });
});
